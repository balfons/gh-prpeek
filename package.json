{
  "name": "gh-prpeek",
  "module": "index.ts",
  "type": "module",
  "version": "3.0.0",
  "scripts": {
    "start": "bun run index.ts",
    "compile": "bun build ./index.ts --compile --outfile $(echo $FILE_PATH)",
    "release": "bun compile && tar -czf dist/gh-prpeek.tar.gz dist/gh-prpeek && shasum -a 256 dist/gh-prpeek.tar.gz"
  },
  "devDependencies": {
    "@types/node-notifier": "^8.0.5",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "boxen": "^7.1.1",
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "node-notifier": "^10.0.1",
    "ora": "^8.0.1",
    "terminal-link": "^3.0.0"
  }
}
